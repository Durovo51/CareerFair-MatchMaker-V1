name: Keep Render App Alive

on:
  schedule:
    - cron: '*/10 * * * *' 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the site
        run: curl -s -o /dev/null -w "%{http_code}" https://your-app.onrender.com
